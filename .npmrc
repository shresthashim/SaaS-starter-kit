# Enable workspace support for monorepos
workspace=true

# Enable strict peer dependency checking (useful for consistent dependency management)
strict-peer-dependencies=true

# Store all dependencies in a single directory in the monorepo root to save space
shared-workspace-lockfile=true